# Orchestrator notes

Short-lived instructions and standing decisions from the user. Not edited by track agents.

## Standing instruction — approval gates

- **2026-08-31:** The user pre-approved autonomous continuation through the wave plan, then
  amended it: **stop after Wave 5 and wait for explicit approval before dispatching Wave 6.**
  Wave 5 (`5-motivation`) was already in flight when this was given; it continues.
  Do not dispatch Wave 6 (`6-network`) without the user saying so.

## Standing human blocker

- `docs/review/contraindications-review.md` needs a human sign-off before release. 32 exercises
  carry no `contraindications[]`; 18 have a suspected missing tag. §13.2 makes this a hard safety
  filter and the pregnancy preset depends on it. Wave 7's brief already names this as a gate.
  The user has agreed to review it — remind them.
